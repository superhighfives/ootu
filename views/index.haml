%section.form
  %form{ :action => "/", :method => "post"}
    %fieldset
      .field
        %label{:for => "url"}
          Enter the YouTube URL
          %input{:type => "text", :name => "url", :class => "text"}
          %input{:type => "submit", :value => "Ootu it!", :class => "button"}

- if @id
  %section.result
    %strong Good news!
    The ID for that YouTube video is 
    %span{:class => "code"}
      = @id

- if @sadface
  %section.sadface
    %strong Oh no.
    Ootu couldn't find an ID from that URL. :(